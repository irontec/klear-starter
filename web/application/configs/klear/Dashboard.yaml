production:
  main:
    module: klearMatrix
    defaultScreen: Dashboard_screen

  screens:
    Dashboard_screen:
      controller: dashboard
      action: index
      title: _("Vista general de la plataforma")
      useExplain: false

testing:
  _extends: production
staging:
  _extends: production
development:
  _extends: production